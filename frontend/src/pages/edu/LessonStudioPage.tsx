import React from "react";
import { useLocation, useNavigate, useParams, useSearchParams } from "react-router-dom";
import { ArrowLeft, ArrowRight, BookOpen, CheckCircle2, ClipboardCheck, LockKeyhole, Play, Plus, Sparkles } from "lucide-react";
import { createTask, generateQuiz, getLesson, saveQuiz, startLessonAttempt, submitQuizAnswers, type Lesson, type Task } from "../../lib/api/edu";
import { getErrorMessageFromUnknown } from "../../lib/safeError";

type Quiz = { question: string; options: Record<string, string>; correct: string };
const preview = () => import.meta.env.DEV && new URLSearchParams(window.location.search).get("preview") === "true";
const shell = "mx-auto max-w-[1260px] px-4 py-7 sm:px-6 lg:px-10 lg:py-10";
const exampleLesson: Lesson & { tasks: Task[] } = { id: 41, type: "LESSON", title: "Колекції: мислити даними", parentTopicTitle: "Python · базовий рівень", hasTheory: true, theory: "# Колекції\n\nКолекція — це спосіб зібрати пов’язані значення так, щоб із ними було зручно працювати разом.\n\n## Сьогодні\n\n- оберемо правильну структуру даних\n- пройдемося по списку\n- зберемо простий словник", tasksCount: 2, createdAt: new Date().toISOString(), tasks: [{ id: 501, title: "Словник частот", description: "Порахуйте, скільки разів кожне слово трапляється у реченні.", template: "", testDataCount: 3, createdAt: new Date().toISOString() }, { id: 502, title: "Мій список покупок", description: "Відфільтруйте й відсортуйте значення без зайвих циклів.", template: "", testDataCount: 2, createdAt: new Date().toISOString() }] };
const Overlay: React.FC<{ title: string; sub?: string; children: React.ReactNode; onClose: () => void }> = ({ title, sub, children, onClose }) => <div className="fixed inset-0 z-[80] overflow-y-auto bg-[#07100a]/45 p-4 backdrop-blur-sm"><section className="mx-auto mt-8 max-w-xl rounded-[30px] bg-[#fbfdfb] p-6 shadow-2xl dark:bg-[#101a13] sm:p-8"><button onClick={onClose} className="float-right grid h-10 w-10 place-items-center rounded-full bg-[#edf2ed] text-xl dark:bg-white/[.07]">×</button><p className="text-xs font-bold uppercase tracking-[.14em] text-[#e17800]">Урок / редактор</p><h2 className="mt-2 font-[family-name:var(--font-display)] text-3xl font-bold tracking-[-.05em]">{title}</h2>{sub && <p className="mt-2 text-sm leading-6 text-[#718075] dark:text-[#a6b4a9]">{sub}</p>}<div className="mt-7 clear-both">{children}</div></section></div>;
const markdownBlocks = (source?: string) => (source || "").split(/\n{2,}/).filter(Boolean).map((block, index) => block.startsWith("#") ? <h2 key={index} className="mt-8 font-[family-name:var(--font-display)] text-2xl font-bold tracking-[-.04em] first:mt-0">{block.replace(/^#+\s*/, "")}</h2> : block.startsWith("-") ? <ul key={index} className="mt-4 space-y-2 text-base leading-7 text-[#405046] dark:text-[#c5d1c8]">{block.split("\n").map((item) => <li key={item} className="flex gap-2"><span className="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-[#00c96e]" />{item.replace(/^-\s*/, "")}</li>)}</ul> : <p key={index} className="mt-4 text-base leading-8 text-[#405046] dark:text-[#c5d1c8]">{block}</p>);

const LessonTeacherStudio: React.FC = () => {
  const { lessonId } = useParams<{ lessonId: string }>(); const id = Number(lessonId); const navigate = useNavigate(); const location = useLocation(); const [params] = useSearchParams(); const lessonType = params.get("type") as "TOPIC" | "CONTROL" | "LESSON" | null; const [lesson, setLesson] = React.useState<(Lesson & { tasks: Task[] }) | null>(null); const [loading, setLoading] = React.useState(true); const [error, setError] = React.useState<string | null>(null); const [modal, setModal] = React.useState<"task" | "quiz" | null>(null); const [busy, setBusy] = React.useState(false); const [task, setTask] = React.useState({ title: "", description: "", template: "" }); const [quiz, setQuiz] = React.useState<Quiz[]>([]); const [answers, setAnswers] = React.useState<Record<number, string>>({}); const [quizState, setQuizState] = React.useState<"idle" | "done">("idle");
  const load = React.useCallback(async () => { setLoading(true); try { setLesson(await getLesson(id, lessonType || undefined)); setError(null); } catch (caught) { if (preview()) setLesson(exampleLesson); else setError(getErrorMessageFromUnknown(caught, "Не вдалося відкрити урок.")); } finally { setLoading(false); } }, [id, lessonType]);
  React.useEffect(() => { void load(); }, [load]);
  React.useEffect(() => { if (!lesson?.quizJson) return; try { const parsed = JSON.parse(lesson.quizJson); if (Array.isArray(parsed)) setQuiz(parsed); } catch { /* invalid legacy payload is intentionally ignored */ } }, [lesson?.quizJson]);
  const create = async () => { if (!task.title.trim()) return; setBusy(true); try { await createTask(id, { title: task.title.trim(), description: task.description.trim(), template: task.template }); setTask({ title: "", description: "", template: "" }); setModal(null); await load(); } catch (caught) { setError(getErrorMessageFromUnknown(caught, "Не вдалося додати практику.")); } finally { setBusy(false); } };
  const createQuiz = async () => { setBusy(true); try { const result = await generateQuiz(id, 8, lesson?.title); setQuiz(result.quiz); await saveQuiz(id, result.quiz); setModal(null); await load(); } catch (caught) { setError(getErrorMessageFromUnknown(caught, "Не вдалося згенерувати квіз.")); } finally { setBusy(false); } };
  const begin = async () => { try { if (!preview()) await startLessonAttempt(id); } catch (caught) { setError(getErrorMessageFromUnknown(caught, "Не вдалося почати спробу.")); } };
  const submit = async () => { if (!quiz.length) return; setBusy(true); try { if (!preview()) await submitQuizAnswers(id, answers, true); setQuizState("done"); } catch (caught) { setError(getErrorMessageFromUnknown(caught, "Не вдалося надіслати відповіді.")); } finally { setBusy(false); } };
  if (loading) return <div className={shell}><div className="h-12 w-64 animate-pulse rounded-xl bg-[#e8eeea] dark:bg-white/[.06]" /><div className="mt-8 h-[650px] animate-pulse rounded-[34px] bg-[#e8eeea] dark:bg-white/[.05]" /></div>;
  if (!lesson) return <div className={shell}>{error || "Урок не знайдено."}</div>;
  const title = lesson.title || "Навчальний блок"; const tasks = lesson.tasks || []; const isControl = lesson.type === "CONTROL" || Boolean(lesson.timeLimitMinutes);
  return <div className={shell}><button onClick={() => { const back = (location.state as { from?: string } | null)?.from; navigate(back || "/edu/lessons"); }} className="mb-6 inline-flex items-center gap-2 text-sm font-bold text-[#617268] transition hover:text-[#16834d] dark:text-[#aab7ad] dark:hover:text-[#72edb0]"><ArrowLeft className="h-4 w-4" />До навчального плану</button><header className="overflow-hidden rounded-[34px] bg-[#f2f6f2] p-6 dark:bg-[#152119] sm:p-10"><div className="flex flex-col justify-between gap-8 xl:flex-row xl:items-end"><div className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#e17800]">{lesson.parentTopicTitle || "Навчальний модуль"}</p><h1 className="mt-3 font-[family-name:var(--font-display)] text-4xl font-bold tracking-[-.065em] sm:text-6xl">{title}</h1><p className="mt-4 max-w-2xl text-base leading-7 text-[#617268] dark:text-[#bfccc3]">{isControl ? "Зосереджена перевірка навичок — без зайвих екранів і несподіванок." : "Пройдіть матеріал у власному темпі, закріпіть його практикою та короткою перевіркою."}</p></div><div className="flex flex-wrap gap-2"><button onClick={() => void begin()} className="rounded-xl bg-[#153321] px-4 py-3 text-sm font-bold text-white dark:bg-[#00d978] dark:text-[#062211]"><Play className="mr-2 inline h-4 w-4" />Почати</button><button onClick={() => setModal("task")} className="rounded-xl border border-[#19291d]/12 px-4 py-3 text-sm font-bold text-[#32443a] dark:border-white/10 dark:text-white"><Plus className="mr-2 inline h-4 w-4" />Практика</button></div></div><div className="mt-10 grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-white/85 px-4 py-4 dark:bg-white/[.06]"><p className="text-xs font-bold uppercase tracking-[.12em] text-[#718075] dark:text-[#a6b4a9]">Матеріал</p><p className="mt-2 text-lg font-bold">{lesson.hasTheory || lesson.theory ? "Готовий до читання" : "Додайте пояснення"}</p></div><div className="rounded-2xl bg-white/85 px-4 py-4 dark:bg-white/[.06]"><p className="text-xs font-bold uppercase tracking-[.12em] text-[#718075] dark:text-[#a6b4a9]">Практика</p><p className="mt-2 text-lg font-bold">{tasks.length} {tasks.length === 1 ? "завдання" : "завдань"}</p></div><div className="rounded-2xl bg-white/85 px-4 py-4 dark:bg-white/[.06]"><p className="text-xs font-bold uppercase tracking-[.12em] text-[#718075] dark:text-[#a6b4a9]">Режим</p><p className="mt-2 text-lg font-bold">{isControl ? `${lesson.timeLimitMinutes || "—"} хв` : "Вільний темп"}</p></div></div></header>{error && <div className="mt-5 rounded-2xl border border-[#ff6b9d]/25 bg-[#ff6b9d]/[.08] px-4 py-3 text-sm text-[#c4436b] dark:text-[#ff9abd]">{error}</div>}<main className="mt-8 grid gap-7 xl:grid-cols-[minmax(0,1fr)_360px]"><article className="rounded-[30px] border border-[#19291d]/10 bg-white p-6 dark:border-white/[.09] dark:bg-[#111b14] sm:p-9"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-2xl bg-[#e7f6ec] text-[#16834d] dark:bg-[#00ff88]/10 dark:text-[#72edb0]"><BookOpen className="h-5 w-5" /></span><div><p className="text-xs font-bold uppercase tracking-[.13em] text-[#16834d] dark:text-[#72edb0]">Пояснення</p><h2 className="mt-1 text-2xl font-bold tracking-[-.04em]">Побудуйте розуміння</h2></div></div><div className="mt-8 max-w-3xl">{lesson.theory ? markdownBlocks(lesson.theory) : <div className="rounded-2xl bg-[#f3f7f3] px-5 py-8 text-center dark:bg-white/[.045]"><Sparkles className="mx-auto h-6 w-6 text-[#e17800]" /><p className="mt-3 text-sm leading-6 text-[#708077] dark:text-[#a6b4a9]">Цей блок поки без теорії. Можна почати з практики або додати матеріал у редакторі уроку.</p></div>}</div></article><aside className="space-y-5"><section className="rounded-[30px] bg-[#163321] p-6 text-white shadow-[0_16px_45px_rgba(13,51,27,.16)] dark:bg-[#1b2b20]"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#7bedb4]">Наступний крок</p><h2 className="mt-3 font-[family-name:var(--font-display)] text-2xl font-bold tracking-[-.04em]">Застосуйте знання</h2><p className="mt-3 text-sm leading-6 text-[#c5d3c9]">Практика одразу після пояснення допомагає зрозуміти, де саме потрібна ще одна спроба.</p><button onClick={() => tasks[0] && navigate(`/edu/tasks/${tasks[0].id}`, { state: { from: location.pathname } })} disabled={!tasks.length} className="mt-5 w-full rounded-xl bg-[#00d978] px-4 py-3 text-sm font-bold text-[#062211] disabled:opacity-40">Відкрити перше завдання <ArrowRight className="ml-1 inline h-4 w-4" /></button></section><section className="rounded-[30px] border border-[#19291d]/10 bg-white p-6 dark:border-white/[.09] dark:bg-[#111b14]"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.13em] text-[#e17800]">Самоперевірка</p><h2 className="mt-1 text-xl font-bold">Квіз</h2></div><ClipboardCheck className="h-5 w-5 text-[#e17800]" /></div>{quiz.length ? <button onClick={() => document.getElementById("lesson-quiz")?.scrollIntoView({ behavior: "smooth" })} className="mt-4 text-sm font-bold text-[#16834d] dark:text-[#72edb0]">{quiz.length} запитань →</button> : <button onClick={() => setModal("quiz")} className="mt-4 text-sm font-bold text-[#16834d] dark:text-[#72edb0]">Створити квіз →</button>}</section></aside></main><section className="mt-8"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#e17800]">Практика</p><h2 className="mt-2 font-[family-name:var(--font-display)] text-3xl font-bold tracking-[-.05em]">Вправи на сьогодні</h2></div><button onClick={() => setModal("task")} className="text-sm font-bold text-[#16834d] dark:text-[#72edb0]">+ Додати</button></div><div className="mt-5 grid gap-3 md:grid-cols-2">{tasks.map((entry, index) => <button key={entry.id} onClick={() => navigate(`/edu/tasks/${entry.id}`, { state: { from: location.pathname } })} className="group rounded-[24px] border border-[#19291d]/10 bg-white p-5 text-left transition hover:-translate-y-0.5 hover:shadow-lg dark:border-white/[.09] dark:bg-[#111b14]"><div className="flex gap-4"><span className="grid h-10 w-10 shrink-0 place-items-center rounded-2xl bg-[#fff1dc] text-sm font-extrabold text-[#a55e00] dark:bg-[#ff8c00]/12 dark:text-[#ffca7e]">{String(index + 1).padStart(2, "0")}</span><div><h3 className="font-bold group-hover:text-[#16834d] dark:group-hover:text-[#72edb0]">{entry.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-[#708077] dark:text-[#a6b4a9]">{entry.description}</p></div></div></button>)}{!tasks.length && <button onClick={() => setModal("task")} className="rounded-[24px] border border-dashed border-[#19291d]/15 px-5 py-12 text-center text-sm font-bold text-[#708077] dark:border-white/10 dark:text-[#a6b4a9]">Додайте першу практику</button>}</div></section>{quiz.length > 0 && <section id="lesson-quiz" className="mt-8 rounded-[30px] border border-[#19291d]/10 bg-white p-6 dark:border-white/[.09] dark:bg-[#111b14] sm:p-9"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.14em] text-[#e17800]">Самоперевірка</p><h2 className="mt-2 font-[family-name:var(--font-display)] text-3xl font-bold tracking-[-.05em]">Що вже стало зрозуміло?</h2></div>{quizState === "done" && <span className="inline-flex items-center gap-2 rounded-full bg-[#e7f6ec] px-3 py-2 text-xs font-bold text-[#16834d] dark:bg-[#00ff88]/10 dark:text-[#72edb0]"><CheckCircle2 className="h-4 w-4" />Відповіді надіслано</span>}</div><div className="mt-7 space-y-6">{quiz.map((question, index) => <fieldset key={`${question.question}-${index}`} className="rounded-2xl bg-[#f3f7f3] p-5 dark:bg-white/[.045]"><legend className="text-base font-bold leading-7">{index + 1}. {question.question}</legend><div className="mt-4 grid gap-2">{Object.entries(question.options || {}).map(([key, value]) => <label key={key} className={`flex cursor-pointer items-center gap-3 rounded-xl px-3 py-3 text-sm transition ${answers[index] === key ? "bg-[#dff6e7] text-[#134c2d] dark:bg-[#00ff88]/12 dark:text-[#a4f4c8]" : "bg-white text-[#415147] dark:bg-[#111b14] dark:text-[#dbe6de]"}`}><input type="radio" name={`q-${index}`} checked={answers[index] === key} onChange={() => setAnswers({ ...answers, [index]: key })} /><span className="font-bold">{key}</span><span>{value}</span></label>)}</div></fieldset>)}</div><button disabled={busy || quizState === "done"} onClick={() => void submit()} className="mt-6 rounded-xl bg-[#153321] px-5 py-3 text-sm font-bold text-white disabled:opacity-40 dark:bg-[#00d978] dark:text-[#062211]">{busy ? "Перевіряємо…" : "Надіслати відповіді"}</button></section>}{modal === "task" && <Overlay title="Нова практика" sub="Нехай задача продовжує логіку уроку, а не дублює теорію." onClose={() => setModal(null)}><div className="space-y-4"><input value={task.title} onChange={(event) => setTask({ ...task, title: event.target.value })} placeholder="Назва завдання" className="w-full rounded-xl border border-[#19291d]/10 bg-white px-4 py-3 text-sm dark:border-white/10 dark:bg-[#0d1710]" /><textarea value={task.description} onChange={(event) => setTask({ ...task, description: event.target.value })} placeholder="Умова" rows={5} className="w-full resize-none rounded-xl border border-[#19291d]/10 bg-white px-4 py-3 text-sm dark:border-white/10 dark:bg-[#0d1710]" /><textarea value={task.template} onChange={(event) => setTask({ ...task, template: event.target.value })} placeholder="Стартовий код (за потреби)" rows={4} className="w-full resize-none rounded-xl border border-[#19291d]/10 bg-white px-4 py-3 font-mono text-sm dark:border-white/10 dark:bg-[#0d1710]" /><button disabled={busy || !task.title.trim()} onClick={() => void create()} className="w-full rounded-xl bg-[#153321] px-4 py-3 font-bold text-white disabled:opacity-40 dark:bg-[#00d978] dark:text-[#062211]">{busy ? "Створюємо…" : "Додати практику"}</button></div></Overlay>}{modal === "quiz" && <Overlay title="Створити квіз" sub="Квіз генерується на основі теми уроку та одразу зберігається у ньому." onClose={() => setModal(null)}><div className="rounded-2xl bg-[#f3f7f3] p-5 dark:bg-white/[.045]"><LockKeyhole className="h-5 w-5 text-[#e17800]" /><p className="mt-3 text-sm leading-6 text-[#617268] dark:text-[#c5d1c8]">Буде створено 8 коротких запитань. Перед публікацією їх можна переглянути у цьому самому уроці.</p></div><button disabled={busy} onClick={() => void createQuiz()} className="mt-5 w-full rounded-xl bg-[#153321] px-4 py-3 font-bold text-white disabled:opacity-40 dark:bg-[#00d978] dark:text-[#062211]">{busy ? "Готуємо…" : "Згенерувати квіз"}</button></Overlay>}</div>;
};

const StudentLessonWorkspace: React.FC = () => {
  const { lessonId } = useParams<{ lessonId: string }>(); const navigate = useNavigate(); const id = Number(lessonId); const [lesson, setLesson] = React.useState<(Lesson & { tasks: Task[] }) | null>(null); const [loading, setLoading] = React.useState(true); const [started, setStarted] = React.useState(false); const [error, setError] = React.useState<string | null>(null);
  React.useEffect(() => { let active = true; getLesson(id).then((value) => { if (active) setLesson(value as Lesson & { tasks: Task[] }); }).catch((caught) => { if (!active) return; if (preview()) setLesson(exampleLesson); else setError(getErrorMessageFromUnknown(caught, "Не вдалося відкрити призначений урок.")); }).finally(() => active && setLoading(false)); return () => { active = false; }; }, [id]);
  if (loading) return <div className={shell}><div className="h-12 w-64 animate-pulse rounded-xl bg-[#e8eeea] dark:bg-white/[.06]" /><div className="mt-8 h-[560px] animate-pulse rounded-[34px] bg-[#e8eeea] dark:bg-white/[.05]" /></div>;
  if (!lesson) return <div className={shell}><p className="rounded-2xl bg-[#ff6b9d]/10 p-5 text-[#c4436b]">{error || "Урок не знайдено."}</p></div>;
  const tasks = lesson.tasks || [];
  return <div className={shell}><button type="button" onClick={() => navigate(`/edu/lessons${preview() ? "?preview=true" : ""}`)} className="mb-6 inline-flex items-center gap-2 text-sm font-bold text-[#617268] hover:text-[#16834d] dark:text-[#aab7ad] dark:hover:text-[#72edb0]"><ArrowLeft className="size-4" />До моїх уроків</button><header className="overflow-hidden rounded-[34px] bg-[#172c20] p-7 text-white sm:p-10"><div className="max-w-3xl"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#83efb6]">Призначений урок · {lesson.parentTopicTitle || "Навчальний модуль"}</p><h1 className="mt-4 font-[family-name:var(--font-display)] text-4xl font-bold tracking-[-.06em] sm:text-6xl">{lesson.title}</h1><p className="mt-5 max-w-2xl text-base leading-7 text-[#bed0c2]">Пройди матеріал, виконай призначену практику й заверши короткою самоперевіркою. Створювати чи змінювати урок не потрібно.</p><button type="button" onClick={async () => { if (!preview()) await startLessonAttempt(id); setStarted(true); }} className="mt-7 rounded-xl bg-[#00ff88] px-5 py-3 text-sm font-bold text-[#062211]">{started ? "Урок розпочато" : "Почати урок"}</button></div></header><main className="mt-7 grid gap-7 xl:grid-cols-[minmax(0,1fr)_340px]"><article className="rounded-[30px] border border-[#19291d]/10 bg-white p-6 dark:border-white/[.09] dark:bg-[#111b14] sm:p-9"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-[#e7f6ec] text-[#16834d] dark:bg-[#00ff88]/10 dark:text-[#72edb0]"><BookOpen className="size-5" /></span><div><p className="text-xs font-bold uppercase tracking-[.13em] text-[#16834d] dark:text-[#72edb0]">Матеріал</p><h2 className="mt-1 text-2xl font-bold">Спершу зрозумій ідею</h2></div></div><div className="mt-8 max-w-3xl">{lesson.theory ? markdownBlocks(lesson.theory) : <p className="text-base leading-8 text-[#617268] dark:text-[#c5d1c8]">Викладач додав практику без окремого текстового матеріалу. Почни з першої задачі.</p>}</div></article><aside className="rounded-[30px] bg-[#fff8e5] p-6 dark:bg-[#ff8c00]/[.08]"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#bd6800] dark:text-[#ffb760]">Твій маршрут</p><ol className="mt-5 space-y-4 text-sm leading-6 text-[#65746a] dark:text-[#c7b795]"><li><b className="mr-2 text-[#bd6800] dark:text-[#ffb760]">01</b>Ознайомся з матеріалом.</li><li><b className="mr-2 text-[#bd6800] dark:text-[#ffb760]">02</b>Виконай {tasks.length} {tasks.length === 1 ? "задачу" : "задачі"}.</li><li><b className="mr-2 text-[#bd6800] dark:text-[#ffb760]">03</b>Повернися до журналу за результатом.</li></ol></aside></main><section className="mt-8"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#e17800]">Призначена практика</p><h2 className="mt-2 font-[family-name:var(--font-display)] text-3xl font-bold tracking-[-.05em]">Завдання уроку</h2><div className="mt-5 grid gap-3 md:grid-cols-2">{tasks.map((task, index) => <button type="button" key={task.id} onClick={() => navigate(`/edu/tasks/${task.id}${preview() ? "?preview=true" : ""}`)} className="group rounded-[24px] border border-[#19291d]/10 bg-white p-5 text-left transition hover:-translate-y-0.5 hover:shadow-lg dark:border-white/[.09] dark:bg-[#111b14]"><span className="grid size-10 place-items-center rounded-2xl bg-[#fff1dc] text-sm font-extrabold text-[#a55e00] dark:bg-[#ff8c00]/12 dark:text-[#ffca7e]">{String(index + 1).padStart(2, "0")}</span><h3 className="mt-5 font-bold group-hover:text-[#16834d] dark:group-hover:text-[#72edb0]">{task.title}</h3><p className="mt-2 line-clamp-2 text-sm leading-6 text-[#708077] dark:text-[#a6b4a9]">{task.description}</p><span className="mt-4 inline-flex items-center gap-1 text-sm font-bold text-[#16834d] dark:text-[#72edb0]">Відкрити <ArrowRight className="size-4" /></span></button>)}{!tasks.length && <div className="rounded-[24px] border border-dashed border-[#19291d]/15 p-8 text-sm text-[#718075] dark:border-white/10 dark:text-[#a6b4a9]">Викладач ще не додав задачі до уроку.</div>}</div></section></div>;
};

export const LessonStudioPage: React.FC<{ student?: boolean }> = ({ student = false }) => student ? <StudentLessonWorkspace /> : <LessonTeacherStudio />;
