import React, { useEffect, useMemo, useState } from "react";
import { useNavigate } from "react-router-dom";
import { ArrowRight, BookOpen, CheckCircle2, Clock3, Target } from "lucide-react";
import { getStudentGrades, getStudentLessons, type Lesson, type Grade } from "../../lib/api/edu";
import type { User } from "../../types";

export const StudentJournalPage: React.FC<{ user: User }> = ({ user }) => {
  const navigate = useNavigate(); const isPreview = import.meta.env.DEV && new URLSearchParams(window.location.search).get("preview") === "true"; const [lessons, setLessons] = useState<Lesson[]>([]); const [grades, setGrades] = useState<Grade[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null);
  useEffect(() => { let active = true; Promise.all([getStudentLessons(), user.studentId ? getStudentGrades(user.studentId) : Promise.resolve({ grades: [] })]).then(([nextLessons, nextGrades]) => { if (!active) return; setLessons(nextLessons); setGrades(nextGrades.grades || []); }).catch((cause: any) => { if (!active) return; if (isPreview) { setLessons([{ id: -51, type: "LESSON", title: "Алгоритми: два вказівники", tasksCount: 3, hasTheory: true, createdAt: "2026-07-10" }, { id: -52, type: "TOPIC", title: "Колекції та словники", tasksCount: 4, hasTheory: true, createdAt: "2026-07-08" }, { id: -53, type: "LESSON", title: "Практика: маленький сервіс", tasksCount: 2, hasTheory: false, createdAt: "2026-07-05", reportOnly: true }] as Lesson[]); setGrades([{ id: -71, total: 88 } as Grade, { id: -72, total: 81 } as Grade, { id: -73, total: 92 } as Grade]); } else setError(String(cause?.response?.data?.message || cause?.message || "Не вдалося завантажити журнал.")); }).finally(() => active && setLoading(false)); return () => { active = false; }; }, [user.studentId, isPreview]);
  const average = useMemo(() => grades.length ? grades.reduce((sum, grade) => sum + Number(grade.total || 0), 0) / grades.length : 0, [grades]);
  const next = lessons.find((lesson) => !lesson.reportOnly) || lessons[0];
  return <div className="min-h-full bg-[#f7f8f5] px-4 py-7 text-[#142017] dark:bg-[#0b120e] dark:text-[#edf3ef] sm:px-6 lg:px-10 lg:py-10"><div className="mx-auto max-w-7xl space-y-6"><section className="rounded-[28px] bg-[#173024] p-6 text-white sm:p-8"><div className="text-xs font-semibold uppercase tracking-[.16em] text-[#72edb0]">Мій журнал</div><div className="mt-4 grid gap-6 lg:grid-cols-[1fr_300px] lg:items-end"><div><h1 className="font-[family-name:var(--font-display)] text-3xl font-bold tracking-[-.055em] sm:text-5xl">Навчання, яке видно.</h1><p className="mt-4 max-w-xl text-base leading-7 text-[#bed1c0]">Тримай свій темп, бач результат і завжди знаєш, що відкрити далі.</p></div><div className="rounded-2xl bg-black/15 p-5"><div className="text-sm text-[#b4c8b7]">Середній результат</div><div className="mt-2 text-5xl font-semibold tracking-[-.07em] text-[#72edb0]">{average ? average.toFixed(0) : "—"}</div><div className="mt-4 h-2 overflow-hidden rounded-full bg-white/10"><div className="h-full rounded-full bg-[#00d977]" style={{ width: `${Math.min(100, average)}%` }} /></div></div></div></section>{error && <div className="rounded-2xl bg-[#fff0f4] p-4 text-sm text-[#bd3c62] dark:bg-[#ff6b9d]/10 dark:text-[#ffa5bf]">{error}</div>}<div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><section className="rounded-[24px] border border-[#152219]/10 bg-white p-5 dark:border-white/10 dark:bg-[#121b15]"><div className="text-xs font-semibold uppercase tracking-[.16em] text-[#147b47] dark:text-[#62ecaa]">Наступний крок</div>{loading ? <div className="mt-5 h-40 animate-pulse rounded-2xl bg-[#edf2ee] dark:bg-white/[.045]" /> : next ? <button onClick={() => navigate(`/edu/lessons/${next.id}`)} className="mt-4 block w-full rounded-2xl bg-[#17251c] p-5 text-left text-white"><div className="flex justify-between"><div><div className="text-xs font-semibold text-[#8eb093]">{next.type}</div><div className="mt-2 text-xl font-semibold">{next.title}</div><div className="mt-2 text-sm text-[#b4c7b7]">{next.tasksCount} задач · {next.hasTheory ? "є теорія" : "практичний блок"}</div></div><ArrowRight className="h-5 w-5 text-[#72edb0]" /></div><div className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-[#72edb0]"><BookOpen className="h-4 w-4" />Відкрити урок</div></button> : <div className="mt-5 rounded-2xl bg-[#f5f8f5] p-5 text-sm text-[#718075] dark:bg-white/[.04] dark:text-[#a4b2a7]">Поки що немає доступних уроків.</div>}</section><section className="rounded-[24px] border border-[#152219]/10 bg-[#fff8ec] p-5 dark:border-[#ff8c00]/20 dark:bg-[#ff8c00]/[.07]"><div className="text-xs font-semibold uppercase tracking-[.16em] text-[#d97706]">Ритм</div><h2 className="mt-2 text-2xl font-semibold tracking-[-.04em]">Твоя активність</h2><div className="mt-5 space-y-3">{lessons.slice(0, 4).map((lesson, index) => <button key={lesson.id} onClick={() => navigate(`/edu/lessons/${lesson.id}`)} className="flex w-full items-center justify-between rounded-xl bg-white/70 p-3 text-left dark:bg-white/[.06]"><div><div className="font-semibold">{lesson.title}</div><div className="mt-1 text-sm text-[#776e5d] dark:text-[#c2b08e]">{lesson.tasksCount} задач</div></div>{lesson.reportOnly ? <CheckCircle2 className="h-4 w-4 text-[#147b47]" /> : <Clock3 className="h-4 w-4 text-[#d97706]" />}</button>)}</div></section></div></div></div>;
};
